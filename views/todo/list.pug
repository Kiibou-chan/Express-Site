extends ../layout

block content
  h1 TODO-List
  a(href='/todo/add') Add Event
  br
  table
    tbody
      tr
        td Time
        td Event
      each val in todoList
        tr
          td #{val.time}
          td #{val.event}
          td 
            a(href=`/todo/edit/${val._id}`) edit
          td
            a(href=`/todo/delete/${val._id}`) delete

